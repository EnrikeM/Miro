# 🔐 Auth Service API

Минимальный сервис аутентификации на ASP.NET Core с использованием JWT, включающий регистрацию и авторизацию пользователей.

Данные пользователей хранятся в базе данных, пароли — в виде SHA-256 хэшей.

## 🧱 Технологии

- **ASP.NET Core** Minimal API
- **Entity Framework Core** + SQL Server
- **JWT Bearer** Authentication
- **Swagger / OpenAPI**
- **SHA-256** hashing

## 📋 Требования

1. [.NET 8 SDK](https://dotnet.microsoft.com/ru-ru/download/dotnet/8.0)
2. **Microsoft SQL Server** (можно установить локально или использовать Docker контейнер)

## 📡 Эндпоинты

### 1️⃣ POST `/api/auth/register` — Регистрация

Создаёт нового пользователя, проверяет уникальность email и валидирует пароль.

#### 📥 Request (JSON)

```json
{
  "email": "user@example.com",
  "password": "string"
}
```

#### 📤 Response `200 OK`

```json
{
  "message": "Пользователь успешно зарегистрирован",
  "userId": "guid"
}
```

#### ❌ Response `400 Bad Request`

**Пользователь уже существует:**
```json
{
  "error": "Пользователь с таким email уже существует"
}
```

**Некорректный email:**
```json
{
  "error": "Некорректный формат email"
}
```

**Слабый пароль:**
```json
{
  "error": "Пароль должен содержать минимум 6 символов"
}
```

---

### 2️⃣ POST `/api/auth/login` — Авторизация

Проверяет email и пароль, возвращает JWT-токен, действительный **24 часа**.

#### 📥 Request (JSON)

```json
{
  "email": "user@example.com",
  "password": "string"
}
```

#### 📤 Response `200 OK`

```json
{
  "token": "jwt-token-string",
  "expiresAt": "2025-03-03T12:34:56Z"
}
```

#### ❌ Response `401 Unauthorized`

```json
{
  "status": 401
}
```

---

## 🔑 Авторизация через JWT

После успешного входа клиент должен передавать токен в заголовке каждого защищённого запроса:

```
Authorization: Bearer {token}
```

### Настройка строки подключения

В файле `appsettings.json` укажите строку подключения к вашей базе данных:

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost;Database=AuthServiceDb;User Id=sa;Password=YourPassword;TrustServerCertificate=True"
  }
}
```
