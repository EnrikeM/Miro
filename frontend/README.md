# 🎨 CU:Create Frontend

Фронтенд для платформы создания и совместного управления проектами, построенный на React и TypeScript. Приложение предоставляет бесконечную доску для работы со стикерами, поддерживает аутентификацию и управление правами доступа.

## ✨ Визуальный стиль и UI

Приложение выполнено в современном стиле с использованием передовых практик веб-дизайна:
- **Glassmorphism**: Эффект матового стекла для панелей инструментов и модальных окон.
- **Адаптивные темы**: Полноценная поддержка **Тёмной** и **Светлой** темы с плавным переключением.
- **Минимализм**: Чистый интерфейс, фокусирующий внимание на контенте.
- **Плавные анимации**: Микровзаимодействия при наведении, появлении элементов и переключении режимов.
- **Сетка**: Динамический фон в точку для улучшения восприятия пространства.

## 🚀 Функционал

### 📋 Бесконечная доска (Infinite Canvas)
- **Навигация**: Масштабирование и перемещение бесконечного полотна.
- **Стикеры**:
  - Создание через двойной клик или drag and drop
  - Перемещение и изменение размера
  - Редактирование текста
  - Изменение цвета стикеров
  - Удаление стикеров

### 🔐 Безопасность и Доступ
- **Аутентификация**:
  - Регистрация и Вход через **JWT**
  - Автоматическое перенаправление на страницу входа для неавторизованных пользователей (Protected Routes)
- **Ролевая модель**:
  - **Owner/Editor**: Полный доступ к редактированию
  - **Viewer**: Режим "Только просмотр". Скрыты инструменты редактирования и кнопка "Поделиться"

## 🛠 Технический стек

- **Core**: React 19, TypeScript, Vite
- **Styling**: Tailwind CSS v4
- **State Management**: React Context (`AuthProvider`, `ThemeProvider`), React Query (`@tanstack/react-query`).
- **Network**: Axios
- **Routing**: React Router v7.

## 📂 Архитектура

Проект следует модульной структуре:

```
src/
├── api/          # Клиент API (Axios)
├── components/   # Переиспользуемые UI компоненты (Sticker, ThemeToggle, и др.)
├── pages/        # Основные страницы (Board, Dashboard, SignIn, SignUp)
├── store/        # Глобальное состояние и контексты (Auth, Theme)
└── types/        # TypeScript определения типов
```

## 🚀 Запуск проекта

1. **Установка зависимостей**:
   ```bash
   npm install
   ```

2. **Настройка окружения**:
   Создайте файл `.env` в корне `frontend` (если нет) и укажите URL бэкенда по примеру:
   ```env
   VITE_API_URL=http://localhost:3000
   ```

3. **Запуск в режиме разработки**:
   ```bash
	 npm install
   npm run dev
   ```

	Приложение будет доступно по адресу: `http://localhost:5173`

4. **Сборка и запуск в продакшн**:
   ```bash
	 npm install
   npm run build
   npm run preview
   ```
	Приложение будет доступно по адресу: `http://localhost:4173`


> P.S. Чтобы запустить проект - полноценно со связью с бэкендом, необходимо запускать через `docker-compose up --build` из корневой директории проекта (при запуске из docker-compose приложение доступно на `http://localhost`)
